#include <stdio.h>
/**
 * main-program that prints the alphabet in lowercase
 * Return: 0
 */
int main(void)
{
	char c;

	c = ('a');

	while (c <= ('z'))
			{
			putchar("*c\n");
			c++;
			}
			return (0);
}
